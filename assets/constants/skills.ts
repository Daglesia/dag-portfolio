import { Skill } from "@/types/props";

export const SkillMap = new Map<string, Skill>([
    ["vuejs", {icon: "logos:vue", displayName: "Vue.js"}],
    ["aws", {icon: "logos:aws", displayName: "AWS"}],
]);